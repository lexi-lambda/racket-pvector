#lang info

(define collection 'multi)

(define name "pvector")
(define version "1.0")

(define deps
  '("base"
    "collections"
    "rackunit-lib"))
(define build-deps
  '("racket-doc"
    "scribble-lib"))
